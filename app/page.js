import HomeClient from './HomeClient';

// Homepage inherits metadata from layout.js (default title)
// No additional metadata needed as layout.js has comprehensive defaults

export default function Home() {
  return <HomeClient />;
}
